Code 1

class Solution {
    public int findClosest(int x, int y, int z) {
        int disX = Math.abs(z - x);
        int disY = Math.abs(z - y);

        if(disX == disY) {
            return 0;
        } 

        return (disX < disY) ? 1 : 2;
    }
}

Time Complexity : O(1)
Space Complexity : 
