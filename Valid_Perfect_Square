Code 

class Solution {
    public boolean isPerfectSquare(int num) {
        long l = 1;
        long r = num;

        while(l <= r) {
            long mid = l + (r - l) / 2; 
            long sqrt = mid * mid;

            if(sqrt == num) {
                return true;
            } else if(sqrt > num) {
                r = mid - 1;
            } else {
                l = mid + 1;
            }
        }

        return false;
    }
}

Time Complexity : O(log n)
Space Complexity : O(1)
