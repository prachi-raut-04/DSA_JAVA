Code 

class Solution {
    public int titleToNumber(String c) {
        int sum = 0;

        for(int i=0; i<c.length(); i++) {
            char ch = c.charAt(i);
            int val = ch - 'A' + 1;
            sum = sum * 26 + val;
        }

        return sum;
    }
}
