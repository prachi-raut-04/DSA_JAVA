Brute Force Approach

class Solution {
    public int singleNumber(int[] nums) {
        int n = nums.length;

        for(int i=0; i<n; i++) {
            boolean isFound = false;
            for(int j=0; j<n; j++) {
                if(j == i) continue;
                else if(nums[i] == nums[j]) {
                    isFound = true;
                }
            }
            if(!isFound) return nums[i];
        }

        return -1;
    }
}
